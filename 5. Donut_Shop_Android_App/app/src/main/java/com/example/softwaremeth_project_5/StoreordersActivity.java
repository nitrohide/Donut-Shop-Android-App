package com.example.softwaremeth_project_5;

import androidx.appcompat.app.AppCompatActivity;
import android.os.Bundle;

/**
 * store orders activity, holds all orders placed from order details, allows users to delete orders placed
 *
 * @author Anuraj Dubey, Chenghao Lin
 */
public class StoreordersActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_storeorders);
    }
}